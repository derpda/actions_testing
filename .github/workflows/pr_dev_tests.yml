on:
  workflow_call:
    inputs:
      commit_sha:
        description: "Commit reference to build and test"
        type: string
        required: true

jobs:
  BuildAndTest:
    # NOTE: Only run if commit_sha is provided.
    if: ${{ inputs.commit_sha != '' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.commit_sha }}
      - name: Build and run tests
        run: |
          echo "Building and running tests for commit: ${{ inputs.commit_sha }}"
          # Add your build and test commands here
          echo "Tests completed successfully."

  Skip:
    # NOTE: Only run if commit_sha is NOT provided.
    if: ${{ inputs.commit_sha == '' }}
    runs-on: ubuntu-latest
    steps:
      - name: Tests skipped
        run: |
          echo "Tests skipped as per request."

  BuildAndTestOrSkip:
    needs: [BuildAndTest, Skip]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    permissions:
      statuses: write
    steps:
      - name: Set commit status
        uses: actions/github-script@v6
        with:
          script: |
            const status = '${{ needs.BuildAndTest.result }}';
            const description = status === 'success' ? 'Tests passed successfully.' :
              status === 'failure' ? 'Tests failed.' : 'Tests were skipped.';
            const state = status === 'success' ? 'success' :
              status === 'failure' ? 'failure' : 'neutral';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: state,
              description: description,
            });
